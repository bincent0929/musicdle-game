# MusicDle

To run the webserver to check how the website is looking run `caddy file-server` in your terminal and you'll be able to open the webpage through going to whatever port the webserver chose on your `localhost`.

Use the files in `/assets/music` for temporary testing of playing music and the logic before using an API.